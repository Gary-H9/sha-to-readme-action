---
name: 🔖 Container Release

on:
  push:
    tags:
      - "*"

permissions:
  contents: write
  packages: write

jobs:
  container-release:
    name: Container Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Set image name to lowercase
        id: image
        run: echo "name=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.image.outputs.name }}:${{ github.ref_name }}
        env:
          DOCKER_BUILD_RECORD_UPLOAD: false

      - name: Update README with release SHA
        run: |
          # Update the SHA badge in README (expects placeholder comments with newlines)
          sed -i '/<!--sha-badge-->/,/<!--\/sha-badge-->/c\<!--sha-badge-->\n![SHA](https://img.shields.io/badge/release%20sha-'"${GITHUB_SHA}"'-blue)\n<!--/sha-badge-->' README.md
          
      - name: Commit updated README
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --staged --quiet || git commit -m "chore: update release SHA to ${GITHUB_SHA}"
          git push